<%- include('../partials/header') -%>
<%- include('../partials/navbar') -%>
      
<div class="container">
    <div class="row mt-4">
        <div class="col-md-3">
            <img src="<%= '/images/' + user.avatar%>" alt="mido" height="150px" width="150px">
        </div>
        <div class="col-md-9  mt-4">
        <h1>Profile</h1>
        <hr>
             <%- include('../partials/flashMessages') -%>

          
            <p>
                <% if (user) {%>

                    Welcome your Email <%= user.email %>
                
                <%}%>
            </p>
            <form action="/users/uploadAvatar" method="POST" enctype="multipart/form-data">
                <input type="file" name="avatar" id="avatar" onchange="readURL(this)">
                <button type="submit" class="btn btn-primary">Upload</button>
            </form>
            <hr>
            <h2 class="display-4"> Image Preview</h2>
            <img sec="#" alt="" id="imagePlaceholder" height="200px" width="200px" style="display:none">

            
        </div>
    </div>
</div>   
<%- include('../partials/footer') -%>

